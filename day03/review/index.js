//alrert prompt, console.log[Input/Output]

//프로그래밍 언어: 컴퓨터[닝겐을 받들기 위한 도구]에게 명령어를 만드는 언어

// 자바스크립트 언어: 브라우저(DOM[HTML + CSS] + 기타)를 조작하기 위한 언어임.

// Variables(변수): const(재할당 불가능), let(재할당 가능), var(절대 쓰면 X)

// Data-Type(타입) :
//  기본 : String[''."".``],Number[숫자], Boolear[ture, false ], Underfined
//  참조 : Array["손흥민", "김민재"], Object {k[문자, 정수]:v[애니타입]}

// Type-Castiong(타입 변환)
// * 문자화 : String(), +[문자열 연결 연산자],
// * 숫자화 : Number(), parseInt()
// * 불리언화: Boolean(), !"탕후루" -> Trysty & Falsy(0, "", null, undefindm [])

// Operator(연산자)
// * 산술연산자: +, -,*,/,%,**
// * 논리연산자: &&(and), ||(or) , !(not)
// * 대입연산자: =
// * 비교연산자: ==, != , >, <, >= , <=
// * 삼항연산자: 조건식 ? 참 : 거짓

// Condition(조건문): if(조건식), else-if(조건식), else
// Funtion(함수)[마술상자]
// funtion name(){}
